syntax = "proto3";

option go_package = "github.com/wwengg/proto/common";

package proto.common;

enum EnumErr {
    SUCCESS = 0;

    // pigeon
    JoinRoomErr = 201;
}

message Empty {

}

message Err {
    EnumErr err = 1;
}